package Suppliers;

import Suppliers.PresentationLayer.CLI;

public class Main {
    public static void main(String[] args) {
        // create new CLI
        CLI cli = new CLI();
        // start menu
        cli.printMenu();
    }
}
